// Generated by ./scripts/test.py:
//
// ./scripts/test.py -c tests/test_paths.toml -o tests/test_paths.t.a.c
//

#include "runners/test_runner.h"

void __test__test_paths_normal__run(__attribute__((unused)) struct lfs_config *cfg) {
    // test case test_paths_normal
    #line 5 "tests/test_paths.toml"
    lfs_t lfs;
    lfs_format(&lfs, cfg) => 0;
    lfs_mount(&lfs, cfg) => 0;
    lfs_mkdir(&lfs, "tea") => 0;
    lfs_mkdir(&lfs, "tea/hottea") => 0;
    lfs_mkdir(&lfs, "tea/warmtea") => 0;
    lfs_mkdir(&lfs, "tea/coldtea") => 0;

    struct lfs_info info;
    lfs_stat(&lfs, "tea/hottea", &info) => 0;
    assert(strcmp(info.name, "hottea") == 0);
    lfs_stat(&lfs, "/tea/hottea", &info) => 0;
    assert(strcmp(info.name, "hottea") == 0);

    lfs_mkdir(&lfs, "/milk") => 0;
    lfs_stat(&lfs, "/milk", &info) => 0;
    assert(strcmp(info.name, "milk") == 0);
    lfs_stat(&lfs, "milk", &info) => 0;
    assert(strcmp(info.name, "milk") == 0);
    lfs_unmount(&lfs) => 0;
    #line 32 "tests/test_paths.t.a.c"
}

void __test__test_paths_redundant_slashes__run(__attribute__((unused)) struct lfs_config *cfg) {
    // test case test_paths_redundant_slashes
    #line 30 "tests/test_paths.toml"
    lfs_t lfs;
    lfs_format(&lfs, cfg) => 0;
    lfs_mount(&lfs, cfg) => 0;
    lfs_mkdir(&lfs, "tea") => 0;
    lfs_mkdir(&lfs, "tea/hottea") => 0;
    lfs_mkdir(&lfs, "tea/warmtea") => 0;
    lfs_mkdir(&lfs, "tea/coldtea") => 0;

    struct lfs_info info;
    lfs_stat(&lfs, "/tea/hottea", &info) => 0;
    assert(strcmp(info.name, "hottea") == 0);
    lfs_stat(&lfs, "//tea//hottea", &info) => 0;
    assert(strcmp(info.name, "hottea") == 0);
    lfs_stat(&lfs, "///tea///hottea", &info) => 0;
    assert(strcmp(info.name, "hottea") == 0);

    lfs_mkdir(&lfs, "////milk") => 0;
    lfs_stat(&lfs, "////milk", &info) => 0;
    assert(strcmp(info.name, "milk") == 0);
    lfs_stat(&lfs, "milk", &info) => 0;
    assert(strcmp(info.name, "milk") == 0);
    lfs_unmount(&lfs) => 0;
    #line 60 "tests/test_paths.t.a.c"
}

void __test__test_paths_dot__run(__attribute__((unused)) struct lfs_config *cfg) {
    // test case test_paths_dot
    #line 57 "tests/test_paths.toml"
    lfs_t lfs;
    lfs_format(&lfs, cfg) => 0;
    lfs_mount(&lfs, cfg) => 0;
    lfs_mkdir(&lfs, "tea") => 0;
    lfs_mkdir(&lfs, "tea/hottea") => 0;
    lfs_mkdir(&lfs, "tea/warmtea") => 0;
    lfs_mkdir(&lfs, "tea/coldtea") => 0;

    struct lfs_info info;
    lfs_stat(&lfs, "./tea/hottea", &info) => 0;
    assert(strcmp(info.name, "hottea") == 0);
    lfs_stat(&lfs, "/./tea/hottea", &info) => 0;
    assert(strcmp(info.name, "hottea") == 0);
    lfs_stat(&lfs, "/././tea/hottea", &info) => 0;
    assert(strcmp(info.name, "hottea") == 0);
    lfs_stat(&lfs, "/./tea/./hottea", &info) => 0;
    assert(strcmp(info.name, "hottea") == 0);

    lfs_mkdir(&lfs, "/./milk") => 0;
    lfs_stat(&lfs, "/./milk", &info) => 0;
    assert(strcmp(info.name, "milk") == 0);
    lfs_stat(&lfs, "milk", &info) => 0;
    assert(strcmp(info.name, "milk") == 0);
    lfs_unmount(&lfs) => 0;
    #line 90 "tests/test_paths.t.a.c"
}

void __test__test_paths_dot_dot__run(__attribute__((unused)) struct lfs_config *cfg) {
    // test case test_paths_dot_dot
    #line 86 "tests/test_paths.toml"
    lfs_t lfs;
    lfs_format(&lfs, cfg) => 0;
    lfs_mount(&lfs, cfg) => 0;
    lfs_mkdir(&lfs, "tea") => 0;
    lfs_mkdir(&lfs, "tea/hottea") => 0;
    lfs_mkdir(&lfs, "tea/warmtea") => 0;
    lfs_mkdir(&lfs, "tea/coldtea") => 0;
    lfs_mkdir(&lfs, "coffee") => 0;
    lfs_mkdir(&lfs, "coffee/hotcoffee") => 0;
    lfs_mkdir(&lfs, "coffee/warmcoffee") => 0;
    lfs_mkdir(&lfs, "coffee/coldcoffee") => 0;

    struct lfs_info info;
    lfs_stat(&lfs, "coffee/../tea/hottea", &info) => 0;
    assert(strcmp(info.name, "hottea") == 0);
    lfs_stat(&lfs, "tea/coldtea/../hottea", &info) => 0;
    assert(strcmp(info.name, "hottea") == 0);
    lfs_stat(&lfs, "coffee/coldcoffee/../../tea/hottea", &info) => 0;
    assert(strcmp(info.name, "hottea") == 0);
    lfs_stat(&lfs, "coffee/../coffee/../tea/hottea", &info) => 0;
    assert(strcmp(info.name, "hottea") == 0);

    lfs_mkdir(&lfs, "coffee/../milk") => 0;
    lfs_stat(&lfs, "coffee/../milk", &info) => 0;
    strcmp(info.name, "milk") => 0;
    lfs_stat(&lfs, "milk", &info) => 0;
    strcmp(info.name, "milk") => 0;
    lfs_unmount(&lfs) => 0;
    #line 124 "tests/test_paths.t.a.c"
}

void __test__test_paths_trailing_dot__run(__attribute__((unused)) struct lfs_config *cfg) {
    // test case test_paths_trailing_dot
    #line 119 "tests/test_paths.toml"
    lfs_t lfs;
    lfs_format(&lfs, cfg) => 0;
    lfs_mount(&lfs, cfg) => 0;
    lfs_mkdir(&lfs, "tea") => 0;
    lfs_mkdir(&lfs, "tea/hottea") => 0;
    lfs_mkdir(&lfs, "tea/warmtea") => 0;
    lfs_mkdir(&lfs, "tea/coldtea") => 0;

    struct lfs_info info;
    lfs_stat(&lfs, "tea/hottea/", &info) => 0;
    assert(strcmp(info.name, "hottea") == 0);
    lfs_stat(&lfs, "tea/hottea/.", &info) => 0;
    assert(strcmp(info.name, "hottea") == 0);
    lfs_stat(&lfs, "tea/hottea/./.", &info) => 0;
    assert(strcmp(info.name, "hottea") == 0);
    lfs_stat(&lfs, "tea/hottea/..", &info) => 0;
    assert(strcmp(info.name, "tea") == 0);
    lfs_stat(&lfs, "tea/hottea/../.", &info) => 0;
    assert(strcmp(info.name, "tea") == 0);
    lfs_unmount(&lfs) => 0;
    #line 150 "tests/test_paths.t.a.c"
}

void __test__test_paths_leading_dot__run(__attribute__((unused)) struct lfs_config *cfg) {
    // test case test_paths_leading_dot
    #line 144 "tests/test_paths.toml"
    lfs_t lfs;
    lfs_format(&lfs, cfg) => 0;
    lfs_mount(&lfs, cfg) => 0;
    lfs_mkdir(&lfs, ".milk") => 0;
    struct lfs_info info;
    lfs_stat(&lfs, ".milk", &info) => 0;
    strcmp(info.name, ".milk") => 0;
    lfs_stat(&lfs, "tea/.././.milk", &info) => 0;
    strcmp(info.name, ".milk") => 0;
    lfs_unmount(&lfs) => 0;
    #line 166 "tests/test_paths.t.a.c"
}

void __test__test_paths_root_dot_dot__run(__attribute__((unused)) struct lfs_config *cfg) {
    // test case test_paths_root_dot_dot
    #line 159 "tests/test_paths.toml"
    lfs_t lfs;
    lfs_format(&lfs, cfg) => 0;
    lfs_mount(&lfs, cfg) => 0;
    lfs_mkdir(&lfs, "tea") => 0;
    lfs_mkdir(&lfs, "tea/hottea") => 0;
    lfs_mkdir(&lfs, "tea/warmtea") => 0;
    lfs_mkdir(&lfs, "tea/coldtea") => 0;
    lfs_mkdir(&lfs, "coffee") => 0;
    lfs_mkdir(&lfs, "coffee/hotcoffee") => 0;
    lfs_mkdir(&lfs, "coffee/warmcoffee") => 0;
    lfs_mkdir(&lfs, "coffee/coldcoffee") => 0;

    struct lfs_info info;
    lfs_stat(&lfs, "coffee/../../../../../../tea/hottea", &info) => 0;
    strcmp(info.name, "hottea") => 0;

    lfs_mkdir(&lfs, "coffee/../../../../../../milk") => 0;
    lfs_stat(&lfs, "coffee/../../../../../../milk", &info) => 0;
    strcmp(info.name, "milk") => 0;
    lfs_stat(&lfs, "milk", &info) => 0;
    strcmp(info.name, "milk") => 0;
    lfs_unmount(&lfs) => 0;
    #line 194 "tests/test_paths.t.a.c"
}

void __test__test_paths_invalid__run(__attribute__((unused)) struct lfs_config *cfg) {
    // test case test_paths_invalid
    #line 186 "tests/test_paths.toml"
    lfs_t lfs;
    lfs_format(&lfs, cfg);
    lfs_mount(&lfs, cfg) => 0;
    struct lfs_info info;
    lfs_stat(&lfs, "dirt", &info) => LFS_ERR_NOENT;
    lfs_stat(&lfs, "dirt/ground", &info) => LFS_ERR_NOENT;
    lfs_stat(&lfs, "dirt/ground/earth", &info) => LFS_ERR_NOENT;

    lfs_remove(&lfs, "dirt") => LFS_ERR_NOENT;
    lfs_remove(&lfs, "dirt/ground") => LFS_ERR_NOENT;
    lfs_remove(&lfs, "dirt/ground/earth") => LFS_ERR_NOENT;

    lfs_mkdir(&lfs, "dirt/ground") => LFS_ERR_NOENT;
    lfs_file_t file;
    lfs_file_open(&lfs, &file, "dirt/ground", LFS_O_WRONLY | LFS_O_CREAT)
            => LFS_ERR_NOENT;
    lfs_mkdir(&lfs, "dirt/ground/earth") => LFS_ERR_NOENT;
    lfs_file_open(&lfs, &file, "dirt/ground/earth", LFS_O_WRONLY | LFS_O_CREAT)
            => LFS_ERR_NOENT;
    lfs_unmount(&lfs) => 0;
    #line 220 "tests/test_paths.t.a.c"
}

void __test__test_paths_root__run(__attribute__((unused)) struct lfs_config *cfg) {
    // test case test_paths_root
    #line 211 "tests/test_paths.toml"
    lfs_t lfs;
    lfs_format(&lfs, cfg) => 0;
    lfs_mount(&lfs, cfg) => 0;
    struct lfs_info info;
    lfs_stat(&lfs, "/", &info) => 0;
    assert(strcmp(info.name, "/") == 0);
    assert(info.type == LFS_TYPE_DIR);

    lfs_mkdir(&lfs, "/") => LFS_ERR_EXIST;
    lfs_file_t file;
    lfs_file_open(&lfs, &file, "/", LFS_O_WRONLY | LFS_O_CREAT)
            => LFS_ERR_ISDIR;

    lfs_remove(&lfs, "/") => LFS_ERR_INVAL;
    lfs_unmount(&lfs) => 0;
    #line 241 "tests/test_paths.t.a.c"
}

void __test__test_paths_root_reprs__run(__attribute__((unused)) struct lfs_config *cfg) {
    // test case test_paths_root_reprs
    #line 231 "tests/test_paths.toml"
    lfs_t lfs;
    lfs_format(&lfs, cfg) => 0;
    lfs_mount(&lfs, cfg) => 0;
    struct lfs_info info;
    lfs_stat(&lfs, "/", &info) => 0;
    assert(strcmp(info.name, "/") == 0);
    assert(info.type == LFS_TYPE_DIR);
    lfs_stat(&lfs, "", &info) => 0;
    assert(strcmp(info.name, "/") == 0);
    assert(info.type == LFS_TYPE_DIR);
    lfs_stat(&lfs, ".", &info) => 0;
    assert(strcmp(info.name, "/") == 0);
    assert(info.type == LFS_TYPE_DIR);
    lfs_stat(&lfs, "..", &info) => 0;
    assert(strcmp(info.name, "/") == 0);
    assert(info.type == LFS_TYPE_DIR);
    lfs_stat(&lfs, "//", &info) => 0;
    assert(strcmp(info.name, "/") == 0);
    assert(info.type == LFS_TYPE_DIR);
    lfs_stat(&lfs, "./", &info) => 0;
    assert(strcmp(info.name, "/") == 0);
    assert(info.type == LFS_TYPE_DIR);
    lfs_unmount(&lfs) => 0;
    #line 270 "tests/test_paths.t.a.c"
}

void __test__test_paths_superblock_conflict__run(__attribute__((unused)) struct lfs_config *cfg) {
    // test case test_paths_superblock_conflict
    #line 259 "tests/test_paths.toml"
    lfs_t lfs;
    lfs_format(&lfs, cfg) => 0;
    lfs_mount(&lfs, cfg) => 0;
    struct lfs_info info;
    lfs_stat(&lfs, "littlefs", &info) => LFS_ERR_NOENT;
    lfs_remove(&lfs, "littlefs") => LFS_ERR_NOENT;

    lfs_mkdir(&lfs, "littlefs") => 0;
    lfs_stat(&lfs, "littlefs", &info) => 0;
    assert(strcmp(info.name, "littlefs") == 0);
    assert(info.type == LFS_TYPE_DIR);
    lfs_remove(&lfs, "littlefs") => 0;
    lfs_stat(&lfs, "littlefs", &info) => LFS_ERR_NOENT;
    lfs_unmount(&lfs) => 0;
    #line 290 "tests/test_paths.t.a.c"
}

void __test__test_paths_max__run(__attribute__((unused)) struct lfs_config *cfg) {
    // test case test_paths_max
    #line 278 "tests/test_paths.toml"
    lfs_t lfs;
    lfs_format(&lfs, cfg) => 0;
    lfs_mount(&lfs, cfg) => 0;
    lfs_mkdir(&lfs, "coffee") => 0;
    lfs_mkdir(&lfs, "coffee/hotcoffee") => 0;
    lfs_mkdir(&lfs, "coffee/warmcoffee") => 0;
    lfs_mkdir(&lfs, "coffee/coldcoffee") => 0;

    char path[1024];
    memset(path, 'w', LFS_NAME_MAX+1);
    path[LFS_NAME_MAX+1] = '\0';
    lfs_mkdir(&lfs, path) => LFS_ERR_NAMETOOLONG;
    lfs_file_t file;
    lfs_file_open(&lfs, &file, path, LFS_O_WRONLY | LFS_O_CREAT)
            => LFS_ERR_NAMETOOLONG;

    memcpy(path, "coffee/", strlen("coffee/"));
    memset(path+strlen("coffee/"), 'w', LFS_NAME_MAX+1);
    path[strlen("coffee/")+LFS_NAME_MAX+1] = '\0';
    lfs_mkdir(&lfs, path) => LFS_ERR_NAMETOOLONG;
    lfs_file_open(&lfs, &file, path, LFS_O_WRONLY | LFS_O_CREAT)
            => LFS_ERR_NAMETOOLONG;
    lfs_unmount(&lfs) => 0;
    #line 319 "tests/test_paths.t.a.c"
}

void __test__test_paths_really_big__run(__attribute__((unused)) struct lfs_config *cfg) {
    // test case test_paths_really_big
    #line 306 "tests/test_paths.toml"
    lfs_t lfs;
    lfs_format(&lfs, cfg) => 0;
    lfs_mount(&lfs, cfg) => 0;
    lfs_mkdir(&lfs, "coffee") => 0;
    lfs_mkdir(&lfs, "coffee/hotcoffee") => 0;
    lfs_mkdir(&lfs, "coffee/warmcoffee") => 0;
    lfs_mkdir(&lfs, "coffee/coldcoffee") => 0;

    char path[1024];
    memset(path, 'w', LFS_NAME_MAX);
    path[LFS_NAME_MAX] = '\0';
    lfs_mkdir(&lfs, path) => 0;
    lfs_remove(&lfs, path) => 0;
    lfs_file_t file;
    lfs_file_open(&lfs, &file, path,
            LFS_O_WRONLY | LFS_O_CREAT) => 0;
    lfs_file_close(&lfs, &file) => 0;
    lfs_remove(&lfs, path) => 0;

    memcpy(path, "coffee/", strlen("coffee/"));
    memset(path+strlen("coffee/"), 'w', LFS_NAME_MAX);
    path[strlen("coffee/")+LFS_NAME_MAX] = '\0';
    lfs_mkdir(&lfs, path) => 0;
    lfs_remove(&lfs, path) => 0;
    lfs_file_open(&lfs, &file, path,
            LFS_O_WRONLY | LFS_O_CREAT) => 0;
    lfs_file_close(&lfs, &file) => 0;
    lfs_remove(&lfs, path) => 0;
    lfs_unmount(&lfs) => 0;
    #line 354 "tests/test_paths.t.a.c"
}

__attribute__((section("_test_suites"), aligned(1)))
const struct test_suite __test__test_paths__suite = {
    .name = "test_paths",
    .path = "tests/test_paths.toml",
    .flags = 0,
    .cases = (const struct test_case[]){
        {
            .name = "test_paths_normal",
            .path = "tests/test_paths.toml:3",
            .flags = 0,
            .permutations = 1,
            .run = __test__test_paths_normal__run,
        },
        {
            .name = "test_paths_redundant_slashes",
            .path = "tests/test_paths.toml:28",
            .flags = 0,
            .permutations = 1,
            .run = __test__test_paths_redundant_slashes__run,
        },
        {
            .name = "test_paths_dot",
            .path = "tests/test_paths.toml:55",
            .flags = 0,
            .permutations = 1,
            .run = __test__test_paths_dot__run,
        },
        {
            .name = "test_paths_dot_dot",
            .path = "tests/test_paths.toml:84",
            .flags = 0,
            .permutations = 1,
            .run = __test__test_paths_dot_dot__run,
        },
        {
            .name = "test_paths_trailing_dot",
            .path = "tests/test_paths.toml:117",
            .flags = 0,
            .permutations = 1,
            .run = __test__test_paths_trailing_dot__run,
        },
        {
            .name = "test_paths_leading_dot",
            .path = "tests/test_paths.toml:142",
            .flags = 0,
            .permutations = 1,
            .run = __test__test_paths_leading_dot__run,
        },
        {
            .name = "test_paths_root_dot_dot",
            .path = "tests/test_paths.toml:157",
            .flags = 0,
            .permutations = 1,
            .run = __test__test_paths_root_dot_dot__run,
        },
        {
            .name = "test_paths_invalid",
            .path = "tests/test_paths.toml:184",
            .flags = 0,
            .permutations = 1,
            .run = __test__test_paths_invalid__run,
        },
        {
            .name = "test_paths_root",
            .path = "tests/test_paths.toml:209",
            .flags = 0,
            .permutations = 1,
            .run = __test__test_paths_root__run,
        },
        {
            .name = "test_paths_root_reprs",
            .path = "tests/test_paths.toml:229",
            .flags = 0,
            .permutations = 1,
            .run = __test__test_paths_root_reprs__run,
        },
        {
            .name = "test_paths_superblock_conflict",
            .path = "tests/test_paths.toml:257",
            .flags = 0,
            .permutations = 1,
            .run = __test__test_paths_superblock_conflict__run,
        },
        {
            .name = "test_paths_max",
            .path = "tests/test_paths.toml:276",
            .flags = 0,
            .permutations = 1,
            .run = __test__test_paths_max__run,
        },
        {
            .name = "test_paths_really_big",
            .path = "tests/test_paths.toml:304",
            .flags = 0,
            .permutations = 1,
            .run = __test__test_paths_really_big__run,
        },
    },
    .case_count = 13,
};

